/*package org.firstinspires.ftc.teamcode.subsystems;

/**
 * Clase Chassis para controlar un chasis diferencial
 * mediante valores de velocidad (no es un modo operativo).
 * Solo maneja las velocidades que se envían a los motores.
 */

/*
public class chassisinopvelocity {

    // Velocidades actuales de los motores (unidades: m/s o rad/s según tu sistema)
    private double leftSpeed  = 0.0;
    private double rightSpeed = 0.0;

    // Límites de seguridad (ajusta según tu robot)
    private static final double MAX_SPEED = 2.0;   // m/s
    private static final double MIN_SPEED = -2.0;  // m/s

    /**
     * Establece la velocidad del motor izquierdo.
     * Se satura automáticamente entre MIN_SPEED y MAX_SPEED.
     */
/*
    public void setLeftSpeed(double speed) {
        this.leftSpeed = saturate(speed);
    }

    /**
     * Establece la velocidad del motor derecho.
     * Se satura automáticamente entre MIN_SPEED y MAX_SPEED.
     */
  /*  public void setRightSpeed(double speed) {
        this.rightSpeed = saturate(speed);
    }

    /**
     * Establece ambas velocidades a la vez (control diferencial).
     */
  /*  public void setSpeeds(double leftSpeed, double rightSpeed) {
        setLeftSpeed(leftSpeed);
        setRightSpeed(rightSpeed);
    }

    /**
     * Detiene el chasis (velocidades a 0).
     */
  /*  public void stop() {
        setSpeeds(0.0, 0.0);
    }

    // ========== Getters ==========

    public double getLeftSpeed() {
        return leftSpeed;
    }

    public double getRightSpeed() {
        return rightSpeed;
    }

    /**
     * Devuelve un array con [leftSpeed, rightSpeed]
     */
  /*  public double[] getSpeeds() {
        return new double[]{leftSpeed, rightSpeed};
    }

    // ========== Utilidades privadas ==========

    private double saturate(double speed) {
        if (speed > MAX_SPEED) return MAX_SPEED;
        if (speed < MIN_SPEED) return MIN_SPEED;
        return speed;
    }

    @Override
    public String toString() {
        return String.format("Chassis{left=%.3f, right=%.3f}", leftSpeed, rightSpeed);
    }
}
*/

//velocidad de el chassis

// ocupabjj
/* */



